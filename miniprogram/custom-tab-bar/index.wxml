<view class="custom-tabbar"><view wx:for="{{items}}" wx:key="path" class="tab-item {{selected === index ? 'is-active' : ''}}" data-index="{{index}}" bindtap="switchTab"><image src="{{selected === index ? item.activeIcon : item.icon}}" mode="aspectFit" /><text>{{item.text}}</text></view></view>
